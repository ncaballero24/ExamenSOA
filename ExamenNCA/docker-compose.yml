version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    networks:
      - examnet
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    networks:
      - examnet

networks:
  examnet:
    driver: bridge
